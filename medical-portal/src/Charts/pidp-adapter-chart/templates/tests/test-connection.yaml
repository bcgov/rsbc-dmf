apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pidp-adapter-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pidp-adapter-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pidp-adapter-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
